import { Oval } from "react-loader-spinner";

export const Loading = () => {
  return <Oval height="100" width="100" color="#0FB968" secondaryColor="#62EB99" ariaLabel="mutating-dots-loading" wrapperStyle={{}} wrapperClass="" visible={true} />;
};
